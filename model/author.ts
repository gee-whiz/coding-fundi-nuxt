export default class Author {
    id: string;
    name: string;
    imageURL: string;
  
    constructor(id: string, name: string, imageURL: string) {
      this.id = id;
      this.name = name;
      this.imageURL = imageURL;
    }
  }
  